# 更新记录

## 0.16.0

- 新增 VS Code Integrated Browser 请求引擎：默认在 VS Code 内置 Chromium 中完成 Cloudflare challenge，并在同一浏览器会话中请求公开 JSON，避免把 Cookie、User-Agent 和浏览器指纹拆开后再次触发 403。
- 新增“自动 / VS Code 原生浏览器 / 手动参数”请求引擎切换；旧版 VS Code 或原生浏览器不可用时，自动模式才回退到已保存的手动请求档案。
- 原生会话使用独立 ephemeral 浏览器存储、精确的 `linux.do` 调试 URL 过滤和 60 KB 响应分块；排除 Cloudflare iframe、blob 和 challenge 子目标。
- 连接完成后多次尝试把焦点还给阅读器，同时保留临时 `VM...` 调试编辑器以避免部分 VS Code 版本终止 CDP 会话；关闭阅读器、清除验证或退出扩展时停止调试连接，并尽力关闭本扩展创建的原生浏览器标签；VS Code 重载后会按需建立新连接。
- 增加原生浏览器能力、Cloudflare 子目标过滤、多块 UTF-8 响应和标签清理测试；原有请求调度、缓存、分享和游戏测试保持通过。

## 0.15.1

- 修正 Cloudflare challenge 与无标记 HTTP 403 的误判：已有游客档案时不再直接提示“验证已失效”，改为 8、15、30 秒的短暂保护退避，成功一次立即清零。
- 临时保护状态与真正的 429/`Retry-After` 服务器限流分开处理，不触发本地 60 秒冷却；首次页面最多自动重试一次，已有页面和最长 6 小时缓存继续保留。
- 参数测试收到 challenge 时明确显示“本次无法判断档案失效”，不会覆盖旧档案；用户可稍后重试，或主动仅保存为未验证档案。
- 旧 SecretStorage 中的 Cookie 与 User-Agent 迁移后允许实际尝试请求，公开接口成功后自动升级为已验证档案，不再强制先重填参数。

## 0.15.0

- Cloudflare 参数改为原子游客请求档案，统一保存白名单 Cookie、User-Agent、允许的客户端提示、来源和验证状态；旧 SecretStorage 分项参数自动迁移为待验证档案。
- 验证设置页支持完整 Request Headers 及 Chrome、Edge、Brave 的 Copy as cURL；cURL 只解析不执行，并拒绝登录 Cookie、Authorization、CSRF、论坛 API 凭据和跨浏览器参数混用。
- 验证固定只测试一次 `/latest.json`；测试失败不覆盖此前有效档案，服务器冷却期间不发送测试请求。
- 移除 60 秒滑动窗口，改用平滑令牌桶：流畅每 4 秒、均衡每 5 秒、稳妥每 8 秒恢复一次请求；导航、手动续载和自动续载使用单并发优先队列。
- 重做 403/429 分类：Cloudflare challenge 直接回到验证提示，明确限流遵守 `Retry-After` 和 RateLimit 预算；无标记 403 不再伪装成 20/60 秒限流，而是报告参数或 TLS/HTTP2 指纹不兼容。
- 缓存改为列表 5 分钟、搜索 2 分钟、分类 30 分钟、主题 30 分钟、帖子续载 60 分钟及 80 条 LRU；冷却或离线时保留最长 6 小时陈旧缓存。
- 自动续载要求新的向下滚动意图，短页面不再连续请求；手动“加载更多”保持可用。
- 阅读器工具栏和活动栏新增“打开加密分享”；强密码生成后支持复制，生成分享后可反复复制分享内容或密码。
- 五款小游戏的选择页、HUD、倒计时、暂停、结果面板、控件、Canvas 和响应式样式抽取为共享 UI，VS Code 与 PyCharm 使用同一份玩法和视觉实现。

## 0.14.0

- 临时分享改为“有效期 + 分享密码”的简化流程，支持自己填写至少 12 个字符的密码或生成 20 位强密码。
- 主题、标题、生成时间和过期时间使用 AES-256-GCM 加密；密码经随机盐和 600,000 次 PBKDF2-HMAC-SHA256 派生密钥。
- 密码不写入分享内容或插件存储，旧版未加密分享内容停止导入。
- 增加 Java/Node 固定加密向量，覆盖错误密码、篡改、过期、未来时间、弱密码和密文元数据不可见。
- VS Code 请求调度改为两次短突发、约 2.2 秒补充一次的令牌节奏，并取消尚未发送的过时导航与分页请求。
- 已成功访问后出现的 403 先按临时限流冷却并逐级降速，避免立即误判为游客参数失效。

## 0.13.0

- 新增最多 60 条的持久化浏览历史，记录公开页面标题、URL 和访问时间。
- 历史界面支持标题/URL 搜索、重新打开、复制 URL 和确认后清空，并适配宽窄编辑器。
- 历史记录会合并同一主题的楼层 URL、过滤 Cloudflare 挑战地址并清理常见站点标题后缀。
- 增加插件内临时分享码教程，明确 Base64URL 编码、SHA-256 校验和与过期时间均不等于加密。
- 增加显式的 VS Code → PyCharm 与 PyCharm → VS Code 固定向量兼容测试。

## 0.12.0

- 所有公开接口请求改为单并发队列，并增加最小请求间隔、同 URL 请求合并和按接口时效缓存。
- 收紧列表与主题的滚动预加载距离，避免短时间连续翻页触发站点限流。
- 403/429 后自动进入冷却期，冷却期间不继续联网；存在最近缓存时继续显示缓存内容。
- 更新错误说明，区分游客参数失效与 Cloudflare 临时限流，避免反复刷新加重限制。

## 0.11.0

- 五款小游戏改用统一共享玩法核心，VS Code 与 PyCharm 的随机数、碰撞、难度和计分保持一致。
- 游戏面板根据可用宽高实时调整，棋盘与高 DPI Canvas 在窗口缩放时不重置游戏状态。
- 2048 增加稳定方块滑动、合并和每局一次撤销；贪吃蛇增加双输入缓冲与平滑移动。
- 公路闪避改为单条连续道路中的按住转向、惯性和擦肩得分；像素跳跃增加长短跳、输入缓冲和土狼时间。
- 扫雷增加首击安全空白区、键盘焦点、长按标记、双击展开和明确的错误旗帜展示。
- 两端统一暂停与结算界面，失焦后保持暂停；不加入音效、粒子或屏幕震动。

## 0.10.0

- VS Code 不再启动、控制或读取外部浏览器；Cloudflare 验证改为扩展内手动参数管理。
- 验证设置页增加 Request Headers 解析、保存并测试、仅保存和详细 DevTools 获取步骤。
- 明确区分 `/latest.json`、主文档和其他接口的请求标头差异，只要求同一次验证中的 Cookie 与 User-Agent。
- PyCharm 工具栏增加宽/中/窄窗口自适应，赛车改为单一道路内的连续转向。

## 0.9.0

- Cloudflare 命令改为直接打开参数管理页，支持从 Request Headers 解析 Cookie 与 User-Agent、逐项清除和全部清除。
- VS Code 不会启动或控制浏览器；手动参数通过公开 `/latest.json` 测试后才更新 SecretStorage，并提供完整的 Windows DevTools 获取说明和示意图。
- 新增当前公开主题的限时分享码，可选 10 分钟、1 小时、24 小时或 7 天，并支持从剪贴板或输入框导入。
- 五款小游戏统一增加开始倒计时、暂停、重新开始、失焦自动暂停和持久化最高分；改进 2048 撤销、贪吃蛇加速、单道路赛车平滑转向、跳跃物理及扫雷首击安全与双击展开。
- 增加共享游戏核心一致性测试与第三方玩法参考声明。

## 0.8.0

- Cloudflare 验证通过后保留独立 Chromium 游客会话，并在阅读器使用期间保持最小化运行。
- 最新、热门、分类、搜索、主题和滚动续载统一改为在已验证浏览器内请求，避免 Cookie 转交 Node.js 后因浏览器指纹变化再次返回 403。
- 只有浏览器内 `/latest.json` 返回有效公开 JSON 后才确认验证成功。
- 浏览器被关闭或验证失效时恢复验证入口；清除验证、关闭阅读器或退出 VS Code 时结束浏览器并清理临时配置。
- 保留原有 SecretStorage 键和游客 Cookie 白名单，继续拒绝登录字段。

## 0.7.0

- 新增默认关闭的休息提醒，可随机等待 31–60 分钟。
- 新增 2048、贪吃蛇、车道闪避、像素跳跃和扫雷。
- 工具栏可切换提醒，也可随时打开小游戏。

## 0.6.0

- 首页、热门和分类主题列表支持滚动续页。
- 列表使用 Discourse 返回的 `more_topics_url`，避免猜测分页规则。
- 返回时恢复点击前的页面 DOM、已加载主题和精确滚动位置，不再重新请求第一页。

## 0.5.0

- 增加阅读器内返回按钮和页面历史记录。
- 主题根据完整帖子索引分批续载，支持滚动到底自动加载和手动加载更多。
- 刷新主题时会重新获取帖子索引与首批内容。
- 默认使用更紧凑、低干扰的 VS Code 原生信息流样式，并可切换舒适密度。

## 0.4.0

- 自动验证会读取临时游客会话所需的 Cloudflare Cookie 和匿名论坛会话 Cookie。
- 手动模式支持粘贴完整 Cookie，但仅保留游客白名单字段。
- 未检测到登录标记时可保留匿名 `_forum_session`；Stripe 和登录相关 Cookie 始终丢弃。

## 0.3.0

- Cloudflare 验证改为自动获取，无需手动复制参数。
- 使用临时 Chrome 游客会话并通过本机调试协议读取 `cf_clearance` 和 User-Agent。
- 验证完成后自动关闭浏览器并删除临时浏览数据。
- 在无法启动兼容浏览器时保留手动粘贴方式。

## 0.2.0

- 增加 Cloudflare 手动验证流程。
- `cf_clearance` 和 User-Agent 使用 VS Code SecretStorage 保存。
- 明确拒绝保存论坛登录 Cookie。

## 0.1.0

- 支持最新、热门、分类和搜索。
- 支持在 VS Code 内阅读公开主题。
- 所有请求均为无 Cookie 的游客请求。
