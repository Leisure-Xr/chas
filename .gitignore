# Local IDE and assistant settings
.claude/
.idea/
.ignore

# macOS metadata
.DS_Store

# Build and dependency output
**/node_modules/
**/build/
**/.gradle/
*.class
*.jar
*.vsix

# The published packages under dist/ are linked from the READMEs by raw URL,
# so they have to stay in the repository like the PyCharm ZIP does.
!linuxdo-guest-browser/dist/*.vsix
